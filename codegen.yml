overwrite: true
schema: "schema.graphql"
documents:
  - "gql/**/*.ts"
generates:
  gql/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
      - "urql-introspection"
